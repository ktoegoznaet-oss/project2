# These are OPTIONAL — leave empty or remove for auto-detection via Nginx.
# Only set if you need to override (e.g., running without Nginx during development).
# NEXT_PUBLIC_API_URL=
# NEXT_PUBLIC_WS_URL=
# NEXT_PUBLIC_STREAM_URL=
